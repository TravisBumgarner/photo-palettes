'use client'

import { createTheme } from '@mui/material'

export const theme = createTheme({
  cssVariables: true,
  palette: {
    mode: 'dark',
    primary: {
      main: '#000000',
    },
    secondary: {
      main: '#000000',
    },
  },
})
