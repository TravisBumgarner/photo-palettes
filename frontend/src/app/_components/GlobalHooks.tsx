'use client'

// This file maybe needs a better home.

import useSignupCode from '../../hooks/useSignupCode'

const GlobalHooks = () => {
  useSignupCode()

  return null
}

export default GlobalHooks
